channels:
  - inbound:
      id: example-server-tcp
      tcp:
        addr: '[::0]:8118'

  - inbound:
      id: example-server-unix
      unix-stream:
        path: near-channels-example-server.sock

default-authn:
